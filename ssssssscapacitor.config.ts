import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'th.ac.ru.uSmart',
  appName: 'th.ac.ru.uSmart',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
